server:
  port: 8083

spring:
  application:
    name: notification-service

management:
  endpoints:
    web:
      exposure:
        include: health, info

employee:
  notification:
    completion-webhook:
      url: http://localhost:8081/api/v1/webhooks/notification-completed